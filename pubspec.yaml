name: survive_ai
description: "Offline-first survival assistant with on-device AI for conflict zones and disaster scenarios."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1


  # SQLite — docs database, FTS5 full-text search, action plan storage
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.4  # for unit tests with in-memory DB

  # File system paths (/files/models/, /files/docs/)
  path_provider: ^2.1.5
  path: ^1.9.1

  # Networking — manifest fetch, resumable model/doc downloads
  http: ^1.4.0

  # WiFi / connectivity detection
  connectivity_plus: ^6.1.4

  # SHA-256 checksum verification
  crypto: ^3.0.6

  # Persistent key-value storage (sync timestamps, first-launch flag, prefs)
  shared_preferences: ^2.5.2

  # Markdown rendering for doc reader screen
  flutter_markdown: ^0.7.6

  # UUID generation for chunk IDs and action plan IDs
  uuid: ^4.5.1
  sqlite3_flutter_libs: ^0.6.0+eol
  flutter_gemma: ^0.12.5

  # ONNX Runtime — the query encoder for the dense retrieval leg.
  # Documents are embedded offline and ship as vectors, so this runs one
  # forward pass over ~10 tokens per turn, not a generation loop.
  flutter_onnxruntime: ^1.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^2.6.2
  build_runner: ^2.4.15
  mockito: ^5.4.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # Directory form: every guide in docs/survival_guides/ is bundled, so
    # adding a new DocTopic only means dropping in the matching .md file.
    - docs/survival_guides/
    # Chunking and embedding run offline in python/ and ship as artifacts, so
    # chunk ids — and therefore citations — are identical on every device and
    # in every eval run, and the phone never embeds a document. Rebuild all
    # four together (they are only meaningful as a set) with:
    #   python3 -m survive_rag pack --tokenizer <path to tokenizer.json>
    - assets/index/corpus.json
    # Passage vectors: float32, passage order, paired by the manifest's ids.
    - assets/index/passages.f32
    - assets/index/passages.f32.json
    # Gemma's BPE tables, pre-resolved to ids so the device tokenizes the
    # query without parsing a 33 MB tokenizer.json.
    - assets/index/tokenizer.bin

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
